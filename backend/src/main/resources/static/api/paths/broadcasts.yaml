post:
  summary: Запуск массовой рассылки
  operationId: sendBroadcast
  tags:
    - Broadcasts
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/schemas/broadcasts/BroadcastRequestDto.yaml'
  responses:
    202:
      description: Рассылка принята
    401:
      $ref: '../components/responses/error/Unauthorized.yaml'
    403:
      $ref: '../components/responses/error/Forbidden.yaml'