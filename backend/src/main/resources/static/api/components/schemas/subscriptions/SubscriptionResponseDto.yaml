type: object
required:
  - id
  - userId
  - startDate
  - endDate
  - status
properties:
  id:
    type: integer
    format: int64
    description: Уникальный ID подписки
  userId:
    type: integer
    format: int64
  startDate:
    type: string
    format: date-time
  endDate:
    type: string
    format: date-time
  status:
    $ref: './SubscriptionStatus.yaml'