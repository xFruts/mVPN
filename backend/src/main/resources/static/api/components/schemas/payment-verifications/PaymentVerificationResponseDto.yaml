type: object
properties:
  id:
    type: integer
    format: int64
  userId:
    type: integer
    format: int64
  billingMonth:
    type: string
    pattern: '^\d{4}-\d{2}$'
  payerFullName:
    type: string
  paidAmount:
    type: number
    format: double
  status:
    $ref: './VerificationStatus.yaml'
  userComment:
    type: string
  adminComment:
    type: string